# 1-Learning-Project-Linear-Model
A simple linear model implemented using PyTorch as an introduction to deep learning. The goal of the project is to understand how a neural network learns the relationship between input features and target values. It covers basic concepts such as forward propagation, loss calculation, backpropagation, and model optimization during Training.
